# Example environment variables (optional)
# Copy to .env and edit as needed.

# Profile selection for CLI commands (local|colab)
PROFILE=local

# MCP server defaults
MCP_HOST=127.0.0.1
MCP_PORT=8000
