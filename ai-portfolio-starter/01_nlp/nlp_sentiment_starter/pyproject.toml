[project]
name = "nlp_sentiment_starter"
version = "0.1.0"
description = "Sentiment analysis starter project (Transformers)."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "pyyaml>=6",
  "transformers>=4.0",
  "torch>=2.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
